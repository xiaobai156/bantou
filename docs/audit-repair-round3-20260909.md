# 第三轮审查修复（2026-09-09）

基线：`709855cf32a42f26a3630038303c1a119e0c025b`。

本轮修复：半头字段和值严格绑定；隐藏脚本/样式不进入正文；专属渲染容器必须唯一；失败重抓和多期汇总严格校验已有成功文件；浏览器每任务使用独立 Context；issue_link 相对所属文档解析；只扫描有资格的 HTML 附属资源；curl 逐跳校验同源；多期模式改为内部参数；HTTP 正文增加 16MiB 与读取截止；缓存比较原始片段；独立验证输出真实来源证据。

新增 Windows Python 3.10/3.11 离线回归，并在 Python 3.11 持续运行本地 Chromium/localhost 冒烟测试。`constraints.txt` 固定直接依赖版本。

没有修改 `sites.json`、`recent_10_cache.json` 或历史结果，没有运行生产全站抓取。动态接口真实分页规则、实际站点结构与当期结果仍需按指定期做只读定向验收。
