# 杀半头_修复版项目专属规则

本项目继承上级 `..\AGENTS.md`。本文件只补充“杀半头_修复版”的专属数据契约、解析边界、缓存边界、判重规则、入口脚本和输出路径，不重复上级统一规则。

## 一、项目范围与正式文件

- 本项目与旧版“杀半头”完全隔离，不得互相读取、补数、写入或共享缓存。
- 正式站点配置：`sites.json`。
- 正式重复检测缓存：`recent_10_cache.json`。
- 单期入口：`bantou_crawler.py`，对应 `爬虫-每天杀半头.bat`。
- 多期入口：`bantou_multi_period.py`，对应 `爬虫-每天杀半头-多期.bat`。
- 正式重复检测入口：`detect_8_consecutive.py`，对应 `爬虫-每天杀半头 - 检测重复.bat`。
- 失败站点定向验证入口：`validate_failed_sites.py`。
- 成功结果必须写入项目同级的 `七类数据统一归纳独立版`。
- 失败结果必须写入项目同级的 `七类数据统一归纳失败独立版`。
- 不得使用或恢复旧路径 `七类数据统一归纳`、`七类数据统一归纳失败` 作为本项目正式输出路径。
- `top_bottom非最高优先级站点.txt` 仅是辅助站点清单，不具有方向裁决权，不能覆盖 `sites.json` 中的 `pick`。

## 二、半头数据契约

- 合法业务值只有：

  `0头单`、`0头双`、`1头单`、`1头双`、`2头单`、`2头双`、`3头单`、`3头双`、`4头单`、`4头双`。

- 数字范围固定为 `0` 至 `4`。
- 每个站点、每一期必须且只能解析一个半头值。
- 半头值必须同时绑定目标期号、半头栏目、站点身份、方向区域和同一原始文档区块。
- 方向边界记录自身同时解析出多个不同合法半头值时必须失败；非边界位置出现同一期不同值不构成冲突，也不得参与边界结果裁决。
- 同一边界记录重复解析出完全相同的候选可以去重；边界记录内标准化后值不同必须保留冲突并判失败。
- 成功结果保留原始候选顺序和原始位置；它们是缓存来源证据，也是修复和冲突判断依据。
- 判重时必须按“具体期号 + 半头值 + 原始位置”共同比较；三项同时一致才算该期相同。当前检测器若仍只比较期号和半头值，属于待修实现冲突，不能据此完成正式判重。

## 三、方向边界与字段边界

- `pick` 只允许 `top` 或 `bottom`。
- 所有站点在同一权威文档、同一目标区块内，先过滤格式不合格候选，再按原始页面顺序执行边界唯一制：`top` 只认第一条合法“期数+数据”，`bottom` 只认最后一条合法“期数+数据”。
- 边界候选不是用户指定期时必须立即失败，禁止继续搜索后续或前面的同期期数，也禁止跨边界补抓。
- 必须先确认权威文档、作者或站名锚点、目标栏目和明确区块，再收集并校验区块内的高可信候选，最后按统一普通站规则应用方向边界；禁止先切整页方向再补做栏目或字段校验。
- `top` 与 `bottom` 不得互换；目标期在另一方向存在，也不能跨方向补抓。
- 主页面、详情页、iframe、script、API、浏览器渲染文档和旧详情页不得无条件合并为一个页面顺序。
- 左半头与右半波必须严格分栏解析。用户指定左侧半头列时，只能读取左列；右侧半波、半波、杀尾、尾数等字段必须完全排除。
- `广东八二` 使用专属解析 `guangdong_baer_left_half_head`，栏目为“『半波半头』”，只取表格左侧单元格；右侧单元格不得参与候选。
- `彩运通` 使用专属解析 `caiyuntong_macau`，只取“澳门综合杀”目标表格区块中的半头字段。
- `深圳福坛` 使用专属解析 `shenzhen_futan_half_head`，URL 固定为 `https://128.241.252.220:9201/tz/bbsjs/080.html`；只认标题“深圳福坛【绝杀半头】”下唯一目标表格，`bottom` 只取该表格最后一条有效“期数+半头”记录。
- 普通站点使用 `strict_half_head`，必须命中配置中的 `anchors`，并通过单期、单值、栏目和正文定位校验。
- `iter_issue_segments` 必须按“同一期 + 同片段内容”去重，双轮扫描不得制造假重复。

## 四、详情页、API与动态来源

- 普通固定页面先读取指定 URL 的原始 HTML；不能从网页最大数字、历史页面或相邻期推断目标期。
- 配置了 `fetch_url` 和 `entry_mode: issue_link` 的站点，必须先在入口页按用户指定期数定位链接，再进入对应详情页；找不到入口即失败。
- 动态 URL 必须从 URL 提取文章、forum 或 reference ID，并优先读取对应专属 API。
- API 返回的记录必须同时满足 URL ID、记录 ID、用户 ID、标题、作者、栏目、正文和目标期一致。
- 用户聚合页没有记录 ID 时，只能通过用户身份、作者、栏目和期数唯一锁定目标记录；多个候选不得按首篇选择。
- 动态正文为空、ID 不一致、作者冲突、栏目冲突或目标记录不唯一，必须失败。
- 只有 API 返回 404，或 200 但记录正文为空壳时，才允许浏览器渲染；网络错误、TLS 错误、500、非可信 JSON、ID 冲突不得触发浏览器兜底。
- 浏览器、iframe、script 和 API 只能提供原始文档；最终仍必须经过专属解析、方向边界和统一校验。
- `南笙一梦`、`安然无恙`、`贵人相助`、`纷纷张开`、`生意兴隆`、`一蹶不振`、`期期大赚` 使用浏览器 `domcontentloaded` 边界；浏览器渲染使用独立线程池，避免多个站点排队误报超时。页面目标区未加载时仍必须失败。
- 每个成功候选必须保存来源 URL、来源类型、记录 ID、记录路径、API URL、标题、作者、原始片段、原始顺序和原始位置。

## 五、专属解析与自适应

- 正式站点必须在 `sites.json` 声明 `parser` 和 `anchors`。
- 已注册解析器包括 `strict_half_head`、`caiyuntong_macau`、`guangdong_baer_left_half_head`、`shenzhen_futan_half_head`。
- 专属解析必须绑定 URL、站点身份、栏目锚点、方向、数据字段和结构边界。
- 专属解析失败后不得回退通用扫描、相邻栏目、其它字段、旧详情页或缓存值凑成功。
- 自适应结构恢复默认关闭，不能以自适应结果决定期号、方向、栏目或业务值。
- 自适应只能用于定位结构，且必须经过隔离影子验证和原专属解析复核后才能启用。
- 当前代码中若仍存在自适应或旧通用兜底路径，不得视为已经满足本规则。

## 六、缓存启动与正式十期边界

- `recent_10_cache.json` 正式重复检测必须是 `schema=2`，包含：
  - `state`；
  - 当前 `period`；
  - 固定 `window=10`；
  - 连续完整 10 期期号；
  - 站点名称、URL、`pick`、解析器和锚点；
  - 每一期的半头值、原始顺序、原始位置及来源证据。
- 启动缓存允许连续 1 至 9 期，只能保存真实抓取成功且证据完整的站点。
- 启动缓存不得用于新增站点正式判重；累计到第 10 个连续期后才转为正式完整缓存。
- 单期抓取先独立完成站点身份、期数、栏目、字段、方向、边界、冲突和数量判断；这些判断不得读取或依赖 `recent_10_cache.json`。
- 单期结果完成后，才允许把本次真实结果独立写入 `recent_10_cache.json`；缓存更新冲突或更新失败不得把已经判定成功的站点改成失败。
- 单期缓存更新不以缓存期数、旧值或缓存冲突作为抓取前置门禁；缓存更新失败必须单独报告，结果 TXT 仍按本次抓取判断写入。
- 正式缓存只允许单期入口更新；多期脚本和连续范围只生成逐期 TXT，不得更新 `recent_10_cache.json`。
- 旧 `schema=1` 缓存不包含来源证据，不能直接作为正式判重依据，也不能伪装成 `schema=2`。
- 用户已明确确认当前 `schema=1` 中的业务值真实可靠时，单期缓存更新可以迁移旧记录；旧期必须标记为 `user-confirmed-legacy`，不得伪造原始位置或页面快照，也不得阻断当次单期抓取。
- 迁移后的旧期仍没有原始来源证据，不能用于正式重复检测，不能与新抓证据混称为同等来源。
- 缓存缺失、期数不连续、身份不一致、来源证据缺失或只有部分站点数据时，检测结果只能是“未完成”，不能判定“不重复”。

## 七、日常新增站点与失败站修复

### 新增站点

新增站点必须依次完成：

1. 同名、同 URL、同 topic、同记录 ID、同栏目和同字段检查。
2. 真实访问指定页面、详情页或 API。
3. 所有站点按首条/末条边界唯一制通过。
4. 通过目标期、半头字段、数量、合法值和唯一性校验。
5. 真实抓取缓存最新期或上一期进行预检。
6. 完成连续近 10 期真实验证。
7. 使用正式检测器和有效 `schema=2` 缓存判重。
8. 完成首次指定期单期抓取，确认成功 TXT、失败 TXT；缓存仅作为抓取完成后的独立记录。
9. 全部通过后才允许写入 `sites.json` 和正式缓存。

近 10 期不完整、无有效候选、来源不可追溯、方向不清、冲突未解决或缓存无效时，必须拒收，不能写成“不重复”。

### 失败站点修复

- 修复只针对用户指定站点、期数、方向和目录。
- 必须真实执行完整单期流程，不能只验证一个正则、函数或模拟字符串。
- 必须覆盖主页面、详情页、API、iframe、script、浏览器渲染、标题正文分离、跨行数据、旧文档干扰和同期冲突。
- 修复成功必须同步已验证期数到 `schema=2` 缓存，并清理该站对应旧失败记录。
- 修复失败必须保留站点、分类、目标期、候选范围、具体原因和网址。
- 任一来源、冲突、输出或缓存环节未验证，只能报告“未完成”。

## 八、正式重复检测

- 正式检测只能使用 `detect_8_consecutive.py` 与有效 `recent_10_cache.json`。
- 不得使用历史 TXT、临时脚本、人工记忆、旧指纹脚本或候选站自行推断的期数。
- 默认比较全局目标期往前的连续 10 期；只能按双方共同拥有的具体期号对齐。
- 缺期不得跨越后拼接连续段；有限替补最多向前 3 期，且替补期必须明确列出，不能冒充主窗口。
- 只有期号相同、半头值相同、原始位置相同且期号连续，才构成连续重复；当前检测器及测试若允许跨站原始位置不同仍判重复，属于待修实现冲突。
- 统一阈值固定为：
  - 连续 1—2 期：不重复；
  - 连续 3—5 期：疑似重复，暂停自动添加并人工审核；
  - 连续 6 期及以上：重复，直接拒收。
- 报告必须逐期输出 `期号:数据`，并同时列出连续起止期、连续长度、双方站点、URL、`pick` 和缓存身份。
- `山高水厂`、`跑狗论坛` 的重复对单独列出，但仍必须逐期报告，不能作为普通统计中的自动放行依据。

## 九、BAT、输出与明确特例

- 单期 BAT 只有手动指定的一个期数可以在结果完成后更新正式缓存；连续范围、连续十期、`--rebuild-cache` 和历史回抓均不得更新正式缓存。`--write-backup` 只表示执行独立缓存记录，不得参与本次抓取裁决。
- 多期 BAT 支持多个期数和连续范围；每期独立生成成功、失败 TXT，多期汇总只列全部指定期失败的站点。
- 多期运行不得刷新正式缓存。
- 单期成功文件命名为 `N期-半头.txt`，失败文件命名为 `N期-半头-失败.txt`。
- 成功文件中站点数据结束后必须单独写一行 `帅铁`，随后紧接排行榜。
- 排行榜标题固定为 `====== 当前排行榜 ======`，列固定为“排名、内容、数量”，不得增加网站名称列。
- 失败 TXT 表头后的站点记录之间必须保留一个空行；每条记录必须包含站点、分类、具体原因和网址。
- 输出、配置和缓存写入必须使用临时文件及原子替换；缓存写入失败必须单独报告，不能回写或污染已经生成的结果 TXT。
- 当前源代码 `bantou/paths.py` 仍指向旧生产输出目录；这是未完成的实现差异，不得在未修正和验证前报告项目已达标。
