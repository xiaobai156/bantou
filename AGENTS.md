# 杀半头_修复版：项目专属规则

本项目继承 `C:\Users\Administrator\Desktop\每天工具\爬虫合集\AGENTS.md`。本文件只记录本项目差异；父级已规定的授权、准确性、来源隔离、缓存事务、原子写入和通用验收要求不在此重复。

## 项目边界与入口

- 本项目与旧版“杀半头”完全隔离，不互读、不补数、不写入、不共享缓存。
- 正式配置：`sites.json`。
- 正式缓存：`recent_10_cache.json`，仅供正式重复检测和结果完成后的近10期滚动更新。
- 单期入口：`bantou_crawler.py`；BAT：`爬虫-每天杀半头.bat`。
- 多期入口：`bantou_multi_period.py`；BAT：`爬虫-每天杀半头-多期.bat`。
- 正式重复检测入口：`detect_8_consecutive.py`；BAT：`爬虫-每天杀半头 - 检测重复.bat`。
- 失败站点验证入口：`validate_failed_sites.py`。
- 成功输出目录：项目同级 `七类数据统一归纳`；失败输出目录：项目同级 `七类数据统一归纳失败`。这两个目录是当前代码 `bantou/paths.py` 实际使用的正式路径。
- `top_bottom非最高优先级站点.txt` 仅作辅助清单，不覆盖 `sites.json` 的 `pick`。

## 半头数据契约

- 合法值仅为：`0头单`、`0头双`、`1头单`、`1头双`、`2头单`、`2头双`、`3头单`、`3头双`、`4头单`、`4头双`。
- 每站每期必须且只能得到一个半头值，并绑定目标期、半头栏目、站点身份、方向区域和同一原始文档区块。
- 同一边界记录中的不同合法值必须判失败；完全相同的重复候选可去重。非边界位置的同期期数不参与边界结果裁决。
- 成功候选须保留原始顺序和位置，作为缓存来源证据。重复检测按双方共同期号对齐；当前正式检测规则以同一期半头值和连续期号为业务比较依据，原始位置用于来源证据核验。

## 方向、栏目与站点解析

- `pick` 只能是 `top` 或 `bottom`。先锁定权威文档、身份锚点、目标栏目和区块，再收集区块内有效候选，最后按配置方向窗口选取；目标期越界即失败。
- 不跨方向、不跨区块、不用其他文档、相邻期、旧详情页或缓存补成功。主页面、详情页、API、iframe、script 和浏览器文档保持来源隔离。
- 左半头和右半波严格分栏。`广东八二` 使用 `guangdong_baer_left_half_head`，只取“『半波半头』”左列。
- `彩运通` 使用 `caiyuntong_macau`，只取“澳门综合杀”区块的半头字段。
- `深圳福坛` 使用 `shenzhen_futan_half_head`，只认标题“深圳福坛【绝杀半头】”下的目标表格；`bottom` 取该表格最后一条有效期数+半头记录。
- 普通站点使用 `strict_half_head`，必须命中 `sites.json` 中的 `anchors`，并通过期数、栏目、正文、字段、数量和唯一性校验。
- 配置 `fetch_url` 与 `entry_mode: issue_link` 时，先在入口页按指定期定位链接，再抓详情页；入口找不到即失败。
- 动态 URL 先校验其中的文章、forum 或 reference ID；API 记录必须与 URL ID、身份、标题、作者、栏目、正文和目标期一致。
- 只有 API 返回404，或返回200但目标正文为空壳时，才允许浏览器渲染；网络、TLS、500、非可信 JSON、ID 冲突或记录不一致不得用浏览器凑成功。
- 以下站点固定使用浏览器 `domcontentloaded` 边界：`南笙一梦`、`安然无恙`、`贵人相助`、`纷纷张开`、`生意兴隆`、`一蹶不振`、`期期大赚`。目标区未加载仍判失败。
- 自适应结构恢复默认关闭；若启用，只能在隔离验证中定位结构，之后仍须通过原专属解析和统一校验，不能决定期数、方向或业务值。

## 缓存与重复检测

- 正式缓存必须是 `schema=2`，包含项目/来源身份、站点名、URL、`pick`、解析器、锚点、期数、值、顺序、位置和来源证据。
- 全局保留连续近10期；站点可只保存真实成功期，缺失期逐期写入该站 `failures`，不得跨期拼接或用旧值冒充成功。
- 单期先完成实时抓取和统一校验，再滚动更新缓存。缓存读取不能参与本次期数、方向、冲突或成功/失败裁决；缓存更新失败须单独报告，不能改写已定稿 TXT。
- 单期入口允许更新正式缓存；多期、连续范围、历史回抓、审计、影子和独立验证不得更新正式缓存。
- 缓存缺失、期数不连续、身份不一致、来源证据不足或仅部分站点有效时，重复检测结果只能为“未完成”。
- 正式重复检测只使用 `detect_8_consecutive.py` 和有效 `recent_10_cache.json`，不使用历史 TXT、临时脚本、人工记忆或旧指纹。
- 连续1至2期相同不算重复；连续3至5期须暂停并人工审核；连续6期及以上拒收。缺期不能跨越形成连续段。

## 新增与失败修复：严格定向

- 新增或修复站点时，绝不运行全站；只运行用户本次明确指定的站点、URL、期数、方向和字段。其他站点的抓取、输出和缓存不得被改写。
- 新增流程按顺序执行：身份检查 → 真实来源确认 → 指定期和方向校验 → 必要历史抓取 → 正式缓存判重 → 指定期单期流程 → 验收 → 才写入配置和缓存。历史不足、来源不可追溯、方向不清、冲突未解决或缓存无效时拒收。
- 修复流程先真实验证指定站点和指定期；只有用户再次明确“开始正式更新”后，才迁移最小改动并定向重跑。验证不修改正式配置、TXT 或缓存。
- 验证通过后，只重跑通过验证的当前期站点；成功才更新该站缓存并清理该站当期失败记录，失败则保留失败记录。不得因一个站点失败而影响其他站点。
- 修复成功写入成功 TXT 时，以原成功内容为只读基线：新站点数据只能追加在网站数据区底部、排行榜之前；原有站点的内容、值、顺序不得覆盖、删除或插入中间。成功 TXT 不再自动追加独立的 `帅铁` 或 `白少华` 行。
- 失败报告保留站点、分类、原因、指定期和网址。缓存和输出均使用项目现有锁、同目录临时文件及原子替换。

## 结果格式与 BAT 约束

- 单期成功文件名为 `N期-半头.txt`，失败文件名为 `N期-半头-失败.txt`。
- 网站数据行使用“半头值 + 一个空格 + 站点目录名”；随后空一行，再输出排行榜。排行榜列为 `内容\t次数\t排名`，并按次数相同共享排名。
- `--retry-fail` 只允许读取用户指定的当期失败文件并定向重跑其中站点；不得把它扩展为全站任务。
- `爬虫-重抓失败站点.bat` 只接受单期，自动读取该期失败 TXT；成功站点清除对应失败行并更新成功结果，仍失败的站点保留失败行。
- `--write-backup` 只表示结果完成后的独立缓存写入，不参与抓取裁决。
- `--rebuild-cache`、多期和历史模式不得更新正式缓存。

## 统一完成标准

按任务范围运行必要的相关测试、Python 编译/静态检查和真实指定期正式流程；不因局部站点修复自动扩大为全项目抓取或全量测试。只有真实来源、输出和缓存均核验通过，才能报告完成；否则报告“未完成”并说明具体环节。

## GitHub 同步规则

- 当前项目的 GitHub 远程为 `origin`；每次本项目修改完成并通过必要验证后，自动创建提交并推送到当前分支的 `origin`。
- 提交前必须保留用户已有未提交改动：只暂存本次任务实际修改的文件和内容，不使用会混入用户改动的全量暂存；无法安全区分时停止提交并报告。
- 不提交或推送凭据、Token、密码、私钥、临时文件、缓存、编译产物、日志和平台元数据；不操作或上传其他项目。
- 推送前核对暂存差异、远程地址和目标分支；提交或推送失败必须如实报告，不得宣称已同步。
